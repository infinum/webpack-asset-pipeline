test('it can be required', () => {
});

test('it emits a file called manifest.json', () => {
});

test('it can specify the file name of emitted file', () => {
});

test('it can specify extraneous files', () => {
});

test('it writes out a file if specified', () => {
});
